<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Your Posted Jobs | Connect</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

  <!-- Animate.css -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>

  <style>
    :root {
      --primary: #4361ee;
      --secondary: #3a0ca3;
      --accent: #4cc9f0;
      --light: #f8f9fa;
      --dark: #212529;
      --transition: all 0.3s ease;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: #f5f7fa;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* ===== Header ===== */
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 60px;
      background: white;
      box-shadow: 0 4px 15px rgba(0,0,0,0.05);
      animation: fadeInDown 1s ease;
      z-index: 1000;
    }
    header .logo {
      display: flex;
      align-items: center;
      font-size: 1.8rem;
      font-weight: bold;
      color: black;
    }
    header .logo svg {
      stroke-dasharray: 300;
      stroke-dashoffset: 300;
      animation: drawLogo 5s ease-in-out infinite;
    }
    header .logo-text {
      margin-left: 10px;
      background: linear-gradient(to right, black, #444);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      font-weight: bold;
    }
    header nav ul {
      list-style: none;
      display: flex;
      gap: 15px;
    }
    header nav ul li a {
      display: inline-block;
      color: black;
      padding: 8px 16px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      transition: var(--transition);
    }
    header nav ul li a:hover {
      background: #333;
      color: #fff;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(0,0,0,0.3);
    }

    /* ===== Layout ===== */
    .layout {
      display: flex;
      flex: 1;
      min-height: calc(100vh - 80px);
    }

    /* ===== Sidebar ===== */
    .sidebar {
      width: 280px;
      background: white;
      padding: 24px;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      box-shadow: 0 0 20px rgba(0,0,0,0.08);
    }
    .nav-section h4 {
      font-size: 14px;
      text-transform: uppercase;
      color: #888;
      margin-bottom: 16px;
      letter-spacing: 0.5px;
    }
    .nav-item {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 16px;
      border-radius: 8px;
      color: #444;
      text-decoration: none;
      margin-bottom: 6px;
      transition: all 0.2s;
    }
    .nav-item:hover { background: #f0f5ff; color: #2255dd; }
    .nav-item.active { background: #2255dd; color: white; }

    .profile {
      margin-top: auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px;
      background: #f9fafc;
      border-radius: 12px;
      box-shadow: 0 2px 6px rgba(0,0,0,.1);
    }
    .profile-info {
      display: flex;
      align-items: center;
    }
    .profile-info img {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      margin-right: 10px;
    }
    .profile .fa-bell {
      font-size: 18px;
      color: #FFD700;
    }

    /* ===== Main Content ===== */
    main {
      flex: 1;
      padding: 30px;
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      overflow-y: auto;
    }

    .container-box {
      max-width: 900px;
      margin: auto;
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      animation: fadeInUp 1s ease;
      transition: all 0.3s ease;
    }
    .container-box:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 35px rgba(0,0,0,0.2);
    }

    h1 {
      color: #2c3e50;
      border-bottom: 2px solid #3498db;
      padding-bottom: 10px;
      margin-bottom: 30px;
    }

    .job-card {
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      margin-bottom: 25px;
      background: white;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .job-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 40px rgba(0,0,0,0.15);
    }
    .job-card-header {
      background: linear-gradient(90deg, #4facfe 0%, #00f2fe 100%);
      color: white;
      padding: 15px 20px;
      border-top-left-radius: 12px;
      border-top-right-radius: 12px;
    }
    .job-card-body { padding: 20px; color: #333; }

    #searchInput {
      width: 100%;
      max-width: 400px;
      margin-bottom: 30px;
      padding: 10px 15px;
      font-size: 1rem;
      border: 2px solid #3498db;
      border-radius: 8px;
      outline: none;
      transition: border-color 0.3s ease;
    }
    #searchInput:focus {
      border-color: #00f2fe;
      box-shadow: 0 0 8px #00f2fe;
    }

    footer {
      background: #2c3e50;
      color: #fff;
      text-align: center;
      padding: 15px;
      margin-top: auto;
    }

    @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes fadeInDown { from { opacity: 0; transform: translateY(-20px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes drawLogo { 0%{stroke-dashoffset:300;}40%{stroke-dashoffset:0;}60%{stroke-dashoffset:0;}100%{stroke-dashoffset:300;} }
  </style>
</head>
<body>

  <!-- ===== Header ===== -->
  <header>
    <div class="logo">
      <svg class="logo-drawing" width="40" height="40" viewBox="0 0 100 100" fill="none">
        <path d="M10 80 L50 20 L90 80 Z" stroke="black" stroke-width="5" fill="transparent"/>
      </svg>
      <span class="logo-text">Connect</span>
    </div>
    <nav>
      <ul>
          <li><a href="/">Home</a></li>
        <li><a href="/company/logout">Log Out</a></li>
      </ul>
    </nav>
  </header>

  <div class="layout">
    <!-- ===== Sidebar ===== -->
    <aside class="sidebar">
      <div class="nav-section">
        <h4>Navigation</h4>
        <a href="/company/postjob" class="nav-item"><i class="fa fa-home"></i> Post-jobs</a>
        <a href="/company/posted" class="nav-item active"><i class="fa fa-briefcase"></i> Posted</a>
     
      </div>
      <div class="nav-section">
        <h4>Applications</h4>
        <a href="/company/applications" class="nav-item "><i class="fa fa-paper-plane"></i> Applications</a>
        <a href="/company/chat_list" class="nav-item"><i class="fa fa-comments"></i> Chats</a>
      </div>
      <div class="profile">
        <div class="profile-info">
          <img  src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" alt="profile">
           <a href="/company">  PROFILE</a>
        </div>
        <i class="fa fa-bell"></i>
      </div>
    </aside>

    <!-- ===== Main Content ===== -->
    <main>
      <div class="container-box">
        <h1>Your Posted Job Opportunities</h1>
        <input type="text" id="searchInput" placeholder="Search jobs..." aria-label="Search jobs" />

        <% if (jobs.length === 0) { %>
          <p>No job opportunities posted yet.</p>
        <% } else { %>
          <% jobs.forEach(job => { %>
  <div class="job-card">
    <div class="job-card-header">
      <h4><%= job.job_title %> (<%= job.opportunity_type %>)</h4>
      <small>Posted on <%= new Date(job.created_at).toLocaleDateString() %></small>
    </div>
    <div class="job-card-body">
      <p><strong>Company:</strong> <%= job.company_name %></p>
      <p><strong>Description:</strong> <%= job.job_description %></p>
      <p><strong>Skills Required:</strong> <%= job.skills %></p>
      <p><strong>Work Arrangement:</strong> <%= job.work_arrangement %></p>
      <p><strong>Office Address:</strong> <%= job.office_address || 'N/A' %></p>
      <p><strong>Working Hours:</strong> <%= job.working_hours || 'N/A' %></p>
      <p><strong>Timezone:</strong> <%= job.timezone || 'N/A' %></p>
      <p><strong>Application Deadline:</strong> <%= job.application_deadline ? new Date(job.application_deadline).toLocaleDateString() : 'N/A' %></p>
      <p><strong>Expected Start Date:</strong> <%= job.start_date ? new Date(job.start_date).toLocaleDateString() : 'N/A' %></p>
      <p><strong>Contact Email:</strong> <%= job.contact_email %></p>
      <p><strong>Salary or Stipend:</strong> <%= job.salary_range || job.stipend_amount || 'N/A' %></p>

      <!-- Edit button -->
      <a href="/company/job/edit/<%= job.id %>" class="btn btn-primary mt-2">
        <i class="fa fa-edit"></i> Edit
      </a>
    </div>
  </div>
<% }) %>
        <% } %>
      </div>
    </main>
  </div>






 
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    // Client-side search/filter script
    const searchInput = document.getElementById('searchInput');
    searchInput.addEventListener('input', function() {
      const filter = this.value.toLowerCase();
      const jobCards = document.querySelectorAll('.job-card');
      jobCards.forEach(card => {
        const text = card.textContent.toLowerCase();
        card.style.display = text.includes(filter) ? '' : 'none';
      });
    });
  </script>

</body>
</html>
