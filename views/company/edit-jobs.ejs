<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Job | Connect</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

  <style>
    :root {
      --primary: #4361ee;
      --secondary: #3a0ca3;
      --accent: #4cc9f0;
      --light: #f8f9fa;
      --dark: #212529;
      --transition: all 0.3s ease;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: #f5f7fa;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* ===== Header ===== */
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 60px;
      background: white;
      box-shadow: 0 4px 15px rgba(0,0,0,0.05);
      animation: fadeInDown 1s ease;
      z-index: 1000;
    }
    header .logo {
      display: flex;
      align-items: center;
      font-size: 1.8rem;
      font-weight: bold;
      color: black;
    }
    header .logo svg {
      stroke-dasharray: 300;
      stroke-dashoffset: 300;
      animation: drawLogo 5s ease-in-out infinite;
    }
    header .logo-text {
      margin-left: 10px;
      background: linear-gradient(to right, black, #444);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      font-weight: bold;
    }
    header nav ul {
      list-style: none;
      display: flex;
      gap: 15px;
    }
    header nav ul li a {
      display: inline-block;
      color: black;
      padding: 8px 16px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      transition: var(--transition);
    }
    header nav ul li a:hover {
      background: #333;
      color: #fff;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(0,0,0,0.3);
    }

    /* ===== Layout ===== */
    .layout {
      display: flex;
      flex: 1;
      min-height: calc(100vh - 80px);
    }

    /* ===== Sidebar ===== */
    .sidebar {
      width: 280px;
      background: white;
      padding: 24px;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      box-shadow: 0 0 20px rgba(0,0,0,0.08);
    }
    .nav-section h4 {
      font-size: 14px;
      text-transform: uppercase;
      color: #888;
      margin-bottom: 16px;
      letter-spacing: 0.5px;
    }
    .nav-item {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 16px;
      border-radius: 8px;
      color: #444;
      text-decoration: none;
      margin-bottom: 6px;
      transition: all 0.2s;
    }
    .nav-item:hover { background: #f0f5ff; color: #2255dd; }
    .nav-item.active { background: #2255dd; color: white; }

    .profile {
      margin-top: auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px;
      background: #f9fafc;
      border-radius: 12px;
      box-shadow: 0 2px 6px rgba(0,0,0,.1);
    }
    .profile-info {
      display: flex;
      align-items: center;
    }
    .profile-info img {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      margin-right: 10px;
    }
    .profile-info a {
      font-weight: 500;
      color: #2255dd;
      text-decoration: none;
    }
    .profile-info a:hover { text-decoration: underline; }
    .profile .fa-bell {
      font-size: 18px;
      color: #FFD700;
    }

    /* ===== Main Content ===== */
    main {
      flex: 1;
      padding: 30px;
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      overflow-y: auto;
    }

    .container-box {
      max-width: 900px;
      margin: auto;
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      animation: fadeInUp 1s ease;
      transition: all 0.3s ease;
    }
    .container-box:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 35px rgba(0,0,0,0.2);
    }

    h2 {
      color: #4361ee;
      text-align: center;
      margin-bottom: 25px;
    }

    .form-control:focus {
      border-color: #4361ee;
      box-shadow: 0 0 5px #4361ee;
    }
    .btn-primary {
      background-color: #4361ee;
      border-color: #4361ee;
    }
    .btn-primary:hover {
      background-color: #3a0ca3;
      border-color: #3a0ca3;
    }

    @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes fadeInDown { from { opacity: 0; transform: translateY(-20px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes drawLogo { 0%{stroke-dashoffset:300;}40%{stroke-dashoffset:0;}60%{stroke-dashoffset:0;}100%{stroke-dashoffset:300;} }
  </style>
</head>
<body>

  <!-- ===== Header ===== -->
  <header>
    <div class="logo">
      <svg class="logo-drawing" width="40" height="40" viewBox="0 0 100 100" fill="none">
        <path d="M10 80 L50 20 L90 80 Z" stroke="black" stroke-width="5" fill="transparent"/>
      </svg>
      <span class="logo-text">Connect</span>
    </div>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/company/logout">Log Out</a></li>
      </ul>
    </nav>
  </header>

  <div class="layout">
    <!-- ===== Sidebar ===== -->
    <aside class="sidebar">
      <div class="nav-section">
        <h4>Navigation</h4>
        <a href="/company/postjob" class="nav-item"><i class="fa fa-home"></i> Post-jobs</a>
        <a href="/company/posted" class="nav-item active"><i class="fa fa-briefcase"></i> Posted</a>
      </div>
      <div class="nav-section">
        <h4>Applications</h4>
        <a href="/company/applications" class="nav-item"><i class="fa fa-paper-plane"></i> Applications</a>
        <a href="/company/chat_list" class="nav-item"><i class="fa fa-comments"></i> Chats</a>
      </div>
      <div class="profile">
        <div class="profile-info">
          <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" alt="profile">
          <a href="/company">PROFILE</a>
        </div>
        <i class="fa fa-bell"></i>
      </div>
    </aside>

    <!-- ===== Main Content ===== -->
    <main>
      <div class="container-box">
        <h2>Edit Job Posting</h2>
        <form action="/company/job/update/<%= job.id %>" method="POST">
          <div class="mb-3">
            <label for="jobTitle" class="form-label">Job Title</label>
            <input type="text" class="form-control" id="jobTitle" name="jobTitle" value="<%= job.job_title %>" required>
          </div>

          <div class="mb-3">
            <label for="jobType" class="form-label">Job Type</label>
            <input type="text" class="form-control" id="jobType" name="jobType" value="<%= job.job_type %>" placeholder="Full-time, Part-time, Internship">
          </div>

          <div class="mb-3">
            <label for="opportunityType" class="form-label">Opportunity Type</label>
            <input type="text" class="form-control" id="opportunityType" name="opportunityType" value="<%= job.opportunity_type %>" placeholder="Remote, Onsite, Hybrid">
          </div>

          <div class="mb-3">
            <label for="jobDescription" class="form-label">Job Description</label>
            <textarea class="form-control" id="jobDescription" name="jobDescription" rows="4" required><%= job.job_description %></textarea>
          </div>

          <div class="mb-3">
            <label for="skills" class="form-label">Skills Required</label>
            <input type="text" class="form-control" id="skills" name="skills" value="<%= job.skills %>" placeholder="Comma separated skills">
          </div>

          <div class="mb-3">
            <label for="salaryRange" class="form-label">Salary Range</label>
            <input type="text" class="form-control" id="salaryRange" name="salaryRange" value="<%= job.salary_range %>" placeholder="e.g., 30000-50000">
          </div>

          <div class="mb-3">
            <label for="stipendAmount" class="form-label">Stipend Amount</label>
            <input type="text" class="form-control" id="stipendAmount" name="stipendAmount" value="<%= job.stipend_amount %>" placeholder="e.g., 10000">
          </div>

          <div class="mb-3">
            <label for="workArrangement" class="form-label">Work Arrangement</label>
            <input type="text" class="form-control" id="workArrangement" name="workArrangement" value="<%= job.work_arrangement %>" placeholder="Remote, Onsite, Hybrid">
          </div>

          <div class="mb-3">
            <label for="officeAddress" class="form-label">Office Address</label>
            <input type="text" class="form-control" id="officeAddress" name="officeAddress" value="<%= job.office_address %>">
          </div>

          <div class="mb-3">
            <label for="workingHours" class="form-label">Working Hours</label>
            <input type="text" class="form-control" id="workingHours" name="workingHours" value="<%= job.working_hours %>" placeholder="e.g., 9 AM - 5 PM">
          </div>

          <div class="mb-3">
            <label for="timezone" class="form-label">Timezone</label>
            <input type="text" class="form-control" id="timezone" name="timezone" value="<%= job.timezone %>">
          </div>

          <div class="mb-3">
            <label for="applicationDeadline" class="form-label">Application Deadline</label>
            <input type="date" class="form-control" id="applicationDeadline" name="applicationDeadline" value="<%= job.application_deadline ? new Date(job.application_deadline).toISOString().split('T')[0] : '' %>">
          </div>

          <div class="mb-3">
            <label for="startDate" class="form-label">Expected Start Date</label>
            <input type="date" class="form-control" id="startDate" name="startDate" value="<%= job.start_date ? new Date(job.start_date).toISOString().split('T')[0] : '' %>">
          </div>

          <div class="mb-3">
            <label for="contactEmail" class="form-label">Contact Email</label>
            <input type="email" class="form-control" id="contactEmail" name="contactEmail" value="<%= job.contact_email %>" required>
          </div>

          <button type="submit" class="btn btn-primary w-100"><i class="fa fa-save"></i> Update Job</button>
        </form>
      </div>
    </main>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
